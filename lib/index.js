import Vue from'vue';import{createShadowDom,updateShadowDom}from'./utils/shadow-dom';export default Vue.directive('badge',{update(a,b){if('relative'!==window.getComputedStyle(a).getPropertyValue('position')&&(console.warn('Component MUST be position: relative, setting then for you :3'),a.style.position='relative'),b.value)if(a.shadowRoot){let c=a.shadowRoot;updateShadowDom(b,c)}else{let c=createShadowDom(a,b);updateShadowDom(b,c)}}});